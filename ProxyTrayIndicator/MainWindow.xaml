<Window x:Name="window" x:Class="ProxyTrayIndicator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProxyTrayIndicator"
        mc:Ignorable="d"
        Title="Edit proxy list" Height="200" Width="400" Visibility="Hidden" ShowInTaskbar="False" MinWidth="400" MinHeight="200">
    <DataGrid Name="dgProxy" HorizontalAlignment="Right" Height="{Binding ActualHeight, ElementName=window, Mode=OneWay}" VerticalAlignment="Bottom" Width="{Binding ActualWidth, ElementName=window, Mode=OneWay}" CanUserResizeRows="False" CanUserReorderColumns="False" Padding="10,40,0,0" MinWidth="{Binding ActualWidth, ElementName=window, Mode=OneWay}" MinHeight="{Binding ActualHeight, ElementName=window, Mode=OneWay}" CanUserAddRows="True" CanUserDeleteRows="True">
        <DataGrid.RowValidationRules>
            <local:ProxyValidationRule ValidationStep="UpdatedValue"/>
        </DataGrid.RowValidationRules>
    </DataGrid>
</Window>
